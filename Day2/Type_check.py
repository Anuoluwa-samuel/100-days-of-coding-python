name = input("what is your name\n") 
length = len(name) 
print("Numbers of letter in your name: " + str(length)) #the purpose of the str was to convert the data type of the variable length to string so that it can be concatenated with the string "Numbers of letter in